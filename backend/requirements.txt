Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1
psycopg2-binary==2.9.9
python-dotenv==1.0.1
requests==2.32.3
# SESSION_233.2 (2026-09-03): bumped from 1.30.5. The old pin predated the
# reasoning-model request shape (max_completion_tokens); every gpt-5-mini
# call paid a retry through extra_body until this moved. dealer_ai.W001
# is the check that fires if this ever slides back.
openai>=1.45,<2
# httpx<0.28 pin from SESSION_059 dropped with it: it only existed because
# openai 1.30.5 passed the removed ``proxies`` kwarg. Newer openai does not.
httpx>=0.27
django-storages[s3]==1.14.6
# Milestone 7 · Increment 1 (SESSION_088) — async infrastructure. VCP
# Phase 6 mandate: "no Celery earlier" — M7 is the first milestone with
# recurring background work to run (floor-plan interest accrual, aging
# snapshots, vendor SLA warnings, photo tombstone reaper). Pins locked
# against Django 5.0.x + Python 3.11:
#   - ``celery==5.5.3`` — mature Django integration. The redis
#     transport is provided by the ``redis`` package pinned below;
#     we drop celery's ``[redis]`` extra per §0.a M20.5 amendment
#     (SESSION_165 CI-cleanup) because kombu's ``[redis]`` bundle
#     pins ``redis<=5.2.1`` and conflicts with the explicit
#     ``redis==6.4.0`` pin on a fresh pip resolution (first surfaced
#     by the M20.1 GitHub Actions acceptance job).
#   - ``django-celery-beat==2.8.1`` — DatabaseScheduler for the Beat
#     schedule; extends the M7.1 Celery app so scheduled entries can be
#     managed via Django admin without touching source.
#   - ``redis==6.4.0`` — Python client used both by Celery transport
#     and by any future direct-cache reads. Runtime compatibility with
#     celery/kombu 5.5.x is proven by the local dev + CI runs; the
#     kombu extra's <=5.2.1 upper bound is a conservative constraint,
#     not a hard incompatibility.
celery==5.5.3
django-celery-beat==2.8.1
redis==6.4.0
