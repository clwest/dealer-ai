# Production build env for the Dealer AI SPA on Vercel.
# Override at deploy-time in the Vercel dashboard if the API host changes.
# The Render service slug below is historical; see render.yaml for the note.
VITE_API_BASE=https://vehicle-match-api.onrender.com/api/dealer-ai
