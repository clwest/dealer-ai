// Vitest global setup — extends the assertion library with
// @testing-library/jest-dom matchers (``toBeInTheDocument`` etc.) so
// every test file can use them without a per-file import.

import "@testing-library/jest-dom/vitest";
