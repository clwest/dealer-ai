<!-- context-kit:adopt:start -->
<!-- This block is managed by `context-kit adopt`. Edits between the markers are overwritten on re-run. Edits outside the markers are preserved. -->

# Dealer OS — Build Plan

> Generated by `context-kit adopt` on 2026-07-31.
> Review the [adopt: please describe] sections before relying on this.

## What this project is

Dealer AI Kit — reusable dealer-facing AI platform shipping Copper Canyon Auto (Yuma, AZ — invented indie, mixed-make used only) as the default persona. Budget-constrained vehicle chat on OpenAI gpt-5-mini (Ollama supported). Deterministic backend math (standard-APR + BHPH weekly/biweekly), 16-stage post-LLM scrub pipeline (incl. indie-prohibited-copy scrub), state-layer enforcement, shadcn UI on Tailwind v3 with brand.* tokens. Franchise dealers supported via env overrides (DEALER_AI_DEALER_TYPE=franchise + DEALER_AI_PRIMARY_MAKE).

## Discovered notes

The user provided these notes from prior inspection or context:

- SESSION_030+031 shipped the independent-dealer pivot end-to-end. Phase 1 (backend behavior surface — DealerProfile resolver, BHPH payment engine, indie-prohibited-copy scrub, Ford-first ranking generalized to primary_make, INDIE_MODE_HINT injection, +43 tests). Phase 2 (Copper Canyon seed data: 45-unit mixed-make used inventory + 4 chat scenarios, +20 tests). Phase 3 (frontend: tailwind ford.* -> brand.*, Copper Canyon palette, sampleInventory.ts, DEFAULT_DEALER populated). Phase 4 (Django backend/freedom_ford -> backend/dealer_kit, git mv + module-path edits, zero regressions). Phase 5 (this session — docs renamed FREEDOM_FORD_* -> DEALER_KIT_*, CAPABILITY_MATRIX + CONTEXT_KIT_INVENTORY + pivot doc refreshed, CLAUDE.md refreshed via context-kit adopt + hand-edit of frontend stack notes brand tokens paragraph). Test baseline 1218 -> 1281 pass, 1 skipped, 0 regressions. Franchise config path preserved via env overrides; Dealer OS demo script and legacy jpg logo intentionally not deleted (kept as alternate-config reference). Deferred: DealerOnboardingProfile migration for the 7 new indie fields, onboarding form extension, useBrand() full-profile exposure, react-router-dom 6->7.

## Tech stack

- **Backend:** Python (detected from `backend/manage.py`)
- **Frontend:** JavaScript / Node.js (detected from `frontend/package.json`)

Manifest files seen: `backend/manage.py`, `backend/requirements.txt`, `frontend/package.json`

> Note: Split monorepo detected. Per-subdir stack listed below; deeper layouts (apps/<name>/...) are not yet handled.

## Adopt Summary

**Type:** Full-stack web app (medium)

**Structure:**

- No workspace child projects detected.

**Reality:** Mixed workspace project (confidence: Medium). _Recognized subdirs (backend, frontend) carry distinct stacks (JavaScript / Node.js, Python)._

**Next actions (3):**

- **[high] Preserve existing context docs before writing** — One or more adopt-managed files already exist without our markers. Adopt will skip them to protect your hand-written content.
- **[medium] Confirm backend/frontend boundaries** — Backend and frontend code appear to live in separate workspaces. Confirm API ownership, local dev startup order, and deployment boundaries.
- **[medium] Classify unrecognized directories** — Confirm the role of 4 unclassified directories, starting with: context/, docs/, future_needs/.

## Agent launch prompt

Copy-paste this into your AI coding agent (Claude Code, Cursor, Aider, etc.) as the first message. Confidence: medium.

```text
You are working in a project that context-kit just adopted. Before writing any code, do the following.

WHAT THIS PROJECT APPEARS TO BE
Full-stack web app (medium confidence)
Reasoning: Recognized split monorepo with a Python backend (backend/) and a JavaScript frontend (frontend/).

USER CONTEXT (provided during adopt)
- Project (per the user): Dealer AI Kit — reusable dealer-facing AI platform shipping Copper Canyon Auto (Yuma, AZ — invented indie, mixed-make used only) as the default persona. Budget-constrained vehicle chat on OpenAI gpt-5-mini (Ollama supported). Deterministic backend math (standard-APR + BHPH weekly/biweekly), 16-stage post-LLM scrub pipeline (incl. indie-prohibited-copy scrub), state-layer enforcement, shadcn UI on Tailwind v3 with brand.* tokens. Franchise dealers supported via env overrides (DEALER_AI_DEALER_TYPE=franchise + DEALER_AI_PRIMARY_MAKE).
- Next task (per the user): SESSION_032 — TBD; likely candidates include DealerOnboardingProfile migration for indie DealerProfile fields (dealer_type, bhph_enabled, subprime_lenders, floor_plan_lender, warranty_offering, credit_range_served, makes_carried) + onboarding form + useBrand() extension; alternatively react-router-dom 6->7 migration or indie counterpart of seed_phase3_demo.

DISCOVERED NOTES / CONTEXT
The user provided these notes from prior inspection or context:
- SESSION_030+031 shipped the independent-dealer pivot end-to-end. Phase 1 (backend behavior surface — DealerProfile resolver, BHPH payment engine, indie-prohibited-copy scrub, Ford-first ranking generalized to primary_make, INDIE_MODE_HINT injection, +43 tests). Phase 2 (Copper Canyon seed data: 45-unit mixed-make used inventory + 4 chat scenarios, +20 tests). Phase 3 (frontend: tailwind ford.* -> brand.*, Copper Canyon palette, sampleInventory.ts, DEFAULT_DEALER populated). Phase 4 (Django backend/freedom_ford -> backend/dealer_kit, git mv + module-path edits, zero regressions). Phase 5 (this session — docs renamed FREEDOM_FORD_* -> DEALER_KIT_*, CAPABILITY_MATRIX + CONTEXT_KIT_INVENTORY + pivot doc refreshed, CLAUDE.md refreshed via context-kit adopt + hand-edit of frontend stack notes brand tokens paragraph). Test baseline 1218 -> 1281 pass, 1 skipped, 0 regressions. Franchise config path preserved via env overrides; Dealer OS demo script and legacy jpg logo intentionally not deleted (kept as alternate-config reference). Deferred: DealerOnboardingProfile migration for the 7 new indie fields, onboarding form extension, useBrand() full-profile exposure, react-router-dom 6->7.

PRIMARY DETECTION
Split monorepo — backend=Python, frontend=JavaScript / Node.js

WORKSPACE / PROJECT STRUCTURE
- No workspace child projects detected.

RECOMMENDED FIRST ACTION
Inspect the backend/frontend boundary first.
- Read the backend code to understand the API surface, models, and ownership.
- Read the frontend workspace to see how it consumes the backend.
- Confirm the local-dev startup order (which side starts first, on what ports) and the deployment boundary.

HOW TO APPROACH THIS REPO
If the project structure is not immediately clear, or the confidence shown above is low, do a structured read-through before proposing tasks: start with entry points (README, main files, routing, configs), map the system (API, frontend, integrations, data flow), and call out inconsistencies, risks, or missing pieces you find.

If confidence is medium, do a quick inspection first — read the README, the top-level structure, and one or two main files — then decide whether the project needs deeper analysis or whether you can move directly to a concrete first task.

If the structure IS clear and confidence is high, skip the read-through and go directly to identifying the highest-value next task — don't pad the response with unnecessary inspection steps.

WHAT TO PRIORITIZE
Prefer identifying real risks, inconsistencies, missing wiring, or unused / incomplete features over surface-level tasks. Do not default to documentation updates unless the user explicitly asked for them.

SAFETY INSTRUCTIONS
- If generated docs exist, read BUILD_PLAN.md, PROJECT_WHAT_IT_IS.md, and CLAUDE.md before making changes. If they do not exist yet, read README, package/manifests, and inspect the detected project structure first.
- Do not change the project's stack (e.g. swapping frameworks, languages, or build tools) without asking the user first.
- Do not run destructive commands (rm -rf, force-push, drop database, schema deletes, etc.).
- Ask before broad refactors that touch more than 3-5 files.
- If the generated docs are missing, do not treat that as an error; continue with repo inspection and propose the first safe task.

TO PRESERVE THIS CONTEXT
After your initial inspection, end your response with a short copy-paste command the user can run to persist this context:

  context-kit adopt . --write \
    --project-summary "<refined one-sentence project summary>" \
    --next-task "<recommended next task>" \
    --notes "<key findings from this inspection>"

When filling in the placeholders:
- Keep notes concise but specific. Include real risks, missing wiring, stale docs, dirty git state, or incomplete features.
- Do not include secrets, tokens, or credentials in any value.
- Escape quotes (\") inside values, or rephrase to avoid quotes.
- If no meaningful findings exist, omit --notes.
- Do not run the command yourself — emit it for the user to copy.

When you're ready to begin work, summarize what you read and propose a concrete first task.
```

### Needs clarification

These directories contain files adopt can see, but it cannot
confidently identify their role yet. Confirm what they are
before making changes.


**Data / content / non-code directories (4):** `context/`, `docs/`, `future_needs/`, `redesign/`

_These contain files but none with extensions adopt categorizes as source (likely config / data / documentation). Listed compactly so the high-signal directories above stay visible._

## Next milestone

SESSION_032 — TBD; likely candidates include DealerOnboardingProfile migration for indie DealerProfile fields (dealer_type, bhph_enabled, subprime_lenders, floor_plan_lender, warranty_offering, credit_range_served, makes_carried) + onboarding form + useBrand() extension; alternatively react-router-dom 6->7 migration or indie counterpart of seed_phase3_demo.

## What NOT to change without asking

- The detected stack above. If the AI proposes switching frameworks,
  ask first.
- Any code outside this `docs/` tree. `adopt` is read-only against
  source.

<!-- context-kit:adopt:end -->
