---
title: "SESSION_145 handoff — Milestone 17 · Increment 1 (M17.1 — Backend: TrialBalanceSnapshot entity + freeze verb + endpoints)"
status: historical
type: handoff
date: 2026-08-02
session: 145
milestone: 17
milestone_status: in-progress
milestone_name: "Trial-balance materialization + as_of picker (monthly-close v1)"
increment: 1
increment_status: shipped
commit: f217e0d
---

# SESSION_145 — Milestone 17 · Increment 1 (M17.1 — Backend: TrialBalanceSnapshot entity + freeze verb + endpoints)

> **Two increments in one session.** Per user direction "commit this
> and continue" after M17.0 planning shipped, M17.1 backend also
> landed in SESSION_145. Both increments preserved as distinct
> commits (`404605e` M17.0 + `f217e0d` M17.1).

## What shipped

Backend-only increment per
`MILESTONE_17_PLANNING.md` §7 M17.1. All
write-path + read-path work for durable
trial-balance snapshots.

**Three §0.a M17.1 micro-decision
recommendations applied at open** (do
not count against planning-time streak
per M10 §9):

1. **Dataclass rename** —
   `TrialBalanceSnapshot` (dataclass) →
   `TrialBalanceComputation`;
   `TrialBalanceRow` →
   `TrialBalanceComputationRow`. Frees
   the "snapshot" name for the durable
   Django model. All call sites updated
   in the same commit
   (`snapshot.py`,
   `services/accounting/__init__.py`,
   `views_accounting.py`,
   `tests/test_m133_trial_balance_service.py`).
2. **Detail endpoint URL** —
   `/admin/accounting/trial-balance/
   snapshots/<int:pk>/`. Pk is the
   canonical identifier; `as_of` is a
   queryable attribute.
3. **Picker default value (M17.2
   concern)** — deferred to M17.2.

## Delivered

**Migration `0046_m171_trial_balance_
snapshot.py`** (auto-generated by
`makemigrations`, reviewed inline):
- Two `CreateModel` operations
  (`TrialBalanceSnapshot` +
  `TrialBalanceSnapshotRow`).
- Two `AddConstraint` operations
  implementing `unique_together` per
  §5.d Option A.
- Zero data migration.

**Two new Django models in `models.py`**
per §5.b Option B:

- `TrialBalanceSnapshot` (header):
  `dealership` FK,
  `as_of DateTimeField`,
  `total_debits` / `total_credits`
  DecimalField(14, 2),
  `is_balanced BooleanField`,
  `created_by` FK to User (nullable),
  `created_at auto_now_add`.
  `Meta.unique_together = (('dealership',
  'as_of'),)`.
  `Meta.ordering = ['-as_of',
  '-created_at']`.
- `TrialBalanceSnapshotRow` (child):
  `dealership` FK,
  `snapshot` FK (CASCADE),
  `account_code` /
  `account_name` /
  `account_type` (using
  `GL_ACCOUNT_TYPE_CHOICES`),
  `debit_total` /
  `credit_total` /
  `natural_balance`
  DecimalField(14, 2),
  `created_at`.
  `Meta.unique_together = (('snapshot',
  'account_code'),)`.
  `Meta.ordering = ['account_code']`.

**New service module
`services/accounting/trial_balance_close.py`**
(~190 lines):

- `freeze_trial_balance(*, dealership,
  as_of, actor) -> TrialBalanceSnapshot`
  — atomic sync-sibling verb per §5.c
  Option A. Calls
  `compute_trial_balance` internally;
  persists header + child rows via
  `bulk_create` in one transaction.
  Catches `IntegrityError` on
  `unique_together` violation +
  re-raises as
  `DuplicateTrialBalanceSnapshotError`
  (409).
- `list_trial_balance_snapshots(*,
  dealership, page=1, page_size=25)
  -> TrialBalanceSnapshotListPage` —
  paginated per M14.1 pattern; ordered
  `-as_of, -created_at, -id`;
  select_related on `created_by`.
- `get_trial_balance_snapshot(*,
  dealership, snapshot_id) ->
  TrialBalanceSnapshot | None` —
  tenant-scoped retrieve; None on
  cross-tenant or missing pk.
- `DuplicateTrialBalanceSnapshotError`
  domain exception (`ValueError`
  subclass — 409 mapping at endpoint
  layer).
- `TrialBalanceSnapshotListPage` frozen
  dataclass matching
  `JournalEntryListPage` shape.

**Extended
`services/accounting/__init__.py`**
`__all__` with the new verbs, error
class, and page dataclass. Also
imported the new symbols from
`.trial_balance_close`. Renamed the
old dataclass imports to
`TrialBalanceComputation` +
`TrialBalanceComputationRow`.

**Three new DRF admin endpoints in
`views_accounting.py`** (~192 lines
added):

- `POST /admin/accounting/trial-
  balance/snapshots/` — freeze.
  Body: `{ "as_of": "<ISO8601>" }`.
  Returns 201 with full snapshot
  projection (header + frozen rows).
  Returns 400 on missing/invalid
  `as_of`, 409 on duplicate, 403 on
  non-permitted role.
- `GET /admin/accounting/trial-
  balance/snapshots/list/` —
  paginated list. Query: `?page=`,
  `?page_size=`. Returns compact
  snapshot summaries (no per-row
  detail). Empty list for zero-
  portfolio tenants (not 404).
- `GET /admin/accounting/trial-
  balance/snapshots/<int:pk>/` —
  detail retrieve. Returns full
  frozen row set. 404 on cross-
  tenant per fail-closed posture.
- All three reuse
  `IsSalesManagerOrOwnerAtActiveDealership`
  (**zero-drift streak extends to
  nine consecutive milestones now**:
  M10 + M11 + M12 + M13 + M14 + M15
  + M16 + M17).

**Tenancy carrier registration** in
`services/tenancy.py`:
`TrialBalanceSnapshot` +
`TrialBalanceSnapshotRow` added to
`_TENANT_CARRIER_MODEL_NAMES`.
Count 47 → **49**.

**Focused tests (37 total)** in new
`tests/test_m171_trial_balance_
materialization.py`:

- `FreezeTrialBalanceHappyPathTests`
  (6 tests): header + child rows,
  totals match computation, actor
  capture, actor optional,
  natural_balance carried, `as_of`
  respected.
- `FreezeTrialBalanceZeroPortfolioTests`
  (1): empty balanced snapshot for a
  zero-postings tenant.
- `FreezeTrialBalanceUniquenessTests`
  (3): duplicate raises
  `DuplicateTrialBalanceSnapshotError`,
  error message names dealer + moment,
  different dealers can share `as_of`.
- `FreezeTrialBalanceAtomicityTests`
  (1): child-row failure rolls back
  header.
- `FreezeTrialBalanceImmutabilityTests`
  (2): frozen rows preserve
  account_name after COA rename;
  backdated JournalEntry does NOT
  change frozen rows (asserts §5.f
  Option A).
- `ListTrialBalanceSnapshotsTests`
  (4): zero-portfolio empty page,
  recent-first ordering, pagination
  bounds, cross-tenant isolation.
- `GetTrialBalanceSnapshotTests` (3):
  returns snapshot in-tenant, None
  for missing pk, None for cross-
  tenant.
- `FreezeEndpointHappyPathTests` (2):
  201 + projection, captures
  authenticated user.
- `FreezeEndpointGuardTests` (4):
  400 missing `as_of`, 400 invalid
  `as_of`, 409 duplicate, 403 non-
  permitted role.
- `SnapshotListEndpointTests` (3):
  zero-portfolio empty, returns list
  after freezes, pagination honoured.
- `SnapshotRetrieveEndpointTests` (3):
  full frozen rows, 404 missing pk,
  404 cross-tenant.
- `M171TenancyCarrierTests` (3):
  snapshot + snapshot row registered,
  count >= 49.
- `M171PermissionClassZeroDriftTests`
  (1): exact permission-class set
  equality (nine-milestone streak).
- `M171EndpointCountTests` (1):
  admin endpoint count >= 107.

## Baseline delta

**Backend: 4,326 → 4,363 pass, 1
skipped, 0 fail** (+37 tests, 0
regressions — in the 30-40 planning
target range). **Frontend Vitest: 122
pass** (unchanged — no frontend at
M17.1). Migrations 0043-0045 →
**0043-0046** (+1). Tenancy carriers
47 → **49**. DRF admin surface 104 →
**107**. Frontend operator routes 20
(unchanged). Permission classes: 7
actual (6 `Is*` + `ReadOnly`) —
**zero drift** at M17.1 (see doc note
below). Celery-beat task families 10
(unchanged — no beat entry per §5.c
Option A sync-sibling shape).

## Doc note: permission-class count

The M16 retrospective and prior
narrative docs recorded the
permission-class count as "8." The
actual class list in
`dealer_ai/permissions.py` has 7 (six
starting with `Is`, plus `ReadOnly`).
M17.1's test class asserts the exact
set for future zero-drift enforcement.
Historical "8" references in
retrospectives are preserved
(historical docs are immutable per
doc governance rule 5); the M17
retrospective at M17.3 will document
the correction going forward.

## Streak update

**70 planning-time as-recommended M5.1
→ M17.0.** No change to the streak
count — M17.1 is implementation-time
work per M10 §9. Three implementation-
time §0.a micro-decisions applied
(dataclass rename, detail URL shape,
picker default deferral) without
counting.

## What's next: SESSION_146 M17.2 frontend

Per `MILESTONE_17_PLANNING.md` §7
M17.2:

- Extend
  `frontend/src/lib/accountingApi.ts`
  with `fetchTrialBalance(asOf?)` +
  `freezeTrialBalance` +
  `listTrialBalanceSnapshots` +
  `fetchTrialBalanceSnapshot` +
  new TypeScript types.
- Install shadcn `Calendar` if not
  present; new
  `TrialBalanceDatePicker`
  component (date-only per §5.e
  Option B; default today per
  §0.a M17.1 decision 2).
- Extend
  `AccountingTrialBalancePage.tsx`
  in place with the date picker +
  "Freeze this view" button +
  "Prior closes" list + inline
  detail view.
- New Vitest coverage for the
  picker, freeze flow, list,
  detail. Frontend Vitest target:
  122 → ~130-138 pass.
- Zero backend changes.

## Anchors that win on conflict

1. `docs/PROJECT_RULES.md`
2. `docs/DOC_GOVERNANCE.md`
3. `docs/roadmap/IMPLEMENTATION_ROADMAP.md`
4. `docs/roadmap/AUTHENTICATION_MODEL.md`
5. `docs/roadmap/MILESTONE_17_PLANNING.md`
   (active memo, includes §7 M17.2)
6. `docs/handoffs/SESSION_145_m17_inc0_planning.md`
   (this session's M17.0 handoff)
7. `docs/CAPABILITY_MATRIX.md` §7q
8. `backend/dealer_ai/services/accounting/trial_balance_close.py`
   (M17.1 service module — pattern
   template for M17.2 frontend
   consumption)
9. `backend/dealer_ai/tests/test_m171_trial_balance_materialization.py`
   (test coverage document)
