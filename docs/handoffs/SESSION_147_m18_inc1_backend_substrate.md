---
title: "SESSION_147 handoff — Milestone 18 · Increment 1 (M18.1 — Backend substrate)"
status: historical
type: handoff
date: 2026-08-02
session: 147
milestone: 18
milestone_status: in-progress
milestone_name: "Demo Store Simulation + Pilot Validation Readiness"
increment: 1
increment_status: shipped
commit: fe9a19a
---

# SESSION_147 — Milestone 18 · Increment 1 (M18.1 — Backend substrate)

## What shipped

Single backend increment per
`MILESTONE_18_PLANNING.md` §7 M18.1. All
M18 substrate — schema + service package
+ guards + TesterFeedback model + management
command + outbound-send-boundary toolkit.

**One §0.a M18.1 implementation-time
decision recorded** (does not count against
planning-time streak per M10 §9):

**Outbound-send-boundary enumeration
finding.** The preliminary list from M18.0
planning was aspirational. Verified at
M18.1 open by grepping `services/` for
`requests.`, `httpx.`, `smtplib.`, `django.
core.mail`, `EMAIL_BACKEND`, and vendor SDK
imports (Twilio / SendGrid / Resend /
Mailgun). Actual state:

- Only two verbs egress to external networks
  today:
  - `services/llm/openai_provider.py::OpenAIProvider.chat`
    (POST to `api.openai.com`)
  - `services/llm/ollama.py::OllamaProvider.chat`
    (POST to local Ollama endpoint)
- Verbs from the preliminary list that do
  NOT exist today: M11.4 follow-up delivery
  (docstring says "deferred"); M12.5
  collection dispatch (records channel/note
  only); M10 F&I lender-portal adapters (not
  implemented); M10 compliance/bureau pulls
  (not implemented); M6/M11 chat outbound
  routing (chat is inbound-only); M9 test-
  drive delivery reminders (not implemented).
- No Django email backend configured; no
  `django.core.mail` usage anywhere.

**Posture chosen at M18.1:** ship the guard
toolkit + a scanner test that fails loud if
any future `services/` verb egresses without
the guard; allowlist the two LLM providers
with a documented rationale ("demo-aware
LLM router deferred to a later 'demo LLM
cost caps' decision — behavior change with
UX implications out of M18.1 scope").
Recorded in M18.6 retrospective §3 deferrals
per this handoff.

## Delivered

**Migration
`0047_m181_demo_store_substrate.py`**
(auto-generated by `makemigrations`;
bundled per M13.1 `accounting_substrate`
precedent — related additions in one
migration):

- `AddField Dealership.is_demo`
  BooleanField(default=False).
- `AddField Dealership.demo_archetype`
  CharField(choices=DEMO_ARCHETYPE_CHOICES,
  blank=True, default="",
  max_length=32).
- `CreateModel TesterFeedback` with all
  required fields + FK to Dealership
  (CASCADE) + choices constraint on
  `category`.

**Vocab constants in `models.py`:**

- `DEMO_ARCHETYPE_RETAIL_SUBPRIME` /
  `DEMO_ARCHETYPE_FLOOR_PLANNED` /
  `DEMO_ARCHETYPE_BHPH` +
  `DEMO_ARCHETYPE_CHOICES` tuple.
- `TESTER_FEEDBACK_CATEGORY_CONFUSION` /
  `TESTER_FEEDBACK_CATEGORY_BUG` /
  `TESTER_FEEDBACK_CATEGORY_FEATURE_REQUEST`
  /
  `TESTER_FEEDBACK_CATEGORY_VALUE_STATEMENT`
  /
  `TESTER_FEEDBACK_CATEGORY_WILLINGNESS_TO_PAY`
  +
  `TESTER_FEEDBACK_CATEGORY_CHOICES` tuple.

**New service package
`services/demo_store/`** (nine modules,
~750 lines):

- `errors.py` —
  `NonDemoResetError(RuntimeError)` domain
  exception per §5.c Option A.
- `outbound_guard.py` — `SuppressedOutbound`
  marker class + `is_demo_dealership()`
  helper + `suppress_if_demo()` canonical
  guard function. Per §5.g Option A + §0.a
  M18.1 decision 1.
- `scenario_summary.py` — `ScenarioSummary`
  frozen dataclass (fields: `archetype`,
  `dealership_id`, `dealership_slug`,
  `seeded_stock_numbers`,
  `seeded_user_usernames`,
  `seeded_scenario_slugs`, `notes`).
- `synthetic_names.py` — 40-name fixed
  pseudonym roster + `get_synthetic_name(index)`
  with modulo wrap.
- `synthetic_data.py` —
  `synthetic_vin(archetype, index)` (17-char
  `DEMO<archetype-code><hex11>`) +
  `synthetic_phone(index)` (`555-01xx`
  NANP) + `synthetic_email(name)`
  (`<slug>@demo.dealer-ai.example`).
- `registry.py` — `create_demo_store()`
  atomic + `reset_demo_store()`
  belt-and-suspenders guarded +
  `list_demo_stores()` pure read +
  `_delete_demo_store_children()` internal
  helper.
- `archetypes/base.py` — `ArchetypeBuilder`
  ABC.
- `archetypes/{retail_subprime,floor_planned,bhph}.py`
  — three stub builders raising
  `NotImplementedError`. Full implementations
  ship at M18.2 / M18.3 / M18.4.
- `archetypes/__init__.py` — dispatcher
  mapping archetype string to builder
  class + `get_archetype_builder()`.
- `__init__.py` — public API `__all__` with
  16 exported symbols.

**Register `TesterFeedback` in
`_TENANT_CARRIER_MODEL_NAMES`** in
`services/tenancy.py`. Count 49 → **50**.

**Management command
`dealer_ai/management/commands/demo_store.py`**
with four subcommands:

- `demo_store create --slug <name>
  --archetype <name> [--display-name
  <name>]`.
- `demo_store reset --slug <name>`.
- `demo_store list`.
- `demo_store export_feedback --dealership
  <slug> [--since <YYYY-MM-DD>] [--out
  <path>]`. CSV writer routes to
  `self.stdout` so tests + operator scripts
  capture output correctly.

**Test helper**
`tests/_auth_helpers.py::make_demo_dealership(archetype,
slug, name=None)` wrapping `make_dealership`
+ setting `is_demo=True` +
`demo_archetype=<value>`.

**53 focused tests** in new
`tests/test_m181_demo_store_substrate.py`:

- Vocab constants exact-set equality (2):
  `DemoArchetypeVocabTests`,
  `TesterFeedbackCategoryVocabTests`.
- Dealership defaults (3):
  `DealershipDemoFieldsTests` — default
  dealership not marked demo; new dealership
  defaults is_demo=False; helper sets both
  fields.
- TesterFeedback model (3):
  `TesterFeedbackModelTests` — create with
  required fields; optional referenced_route;
  tenancy autofill fires on bypass.
- Tenancy carrier registration (2):
  `M181TenancyCarrierTests` — carrier
  registered; count >= 50.
- Synthetic helpers (10):
  `SyntheticVinTests` (5),
  `SyntheticPhoneTests` (2),
  `SyntheticEmailTests` (4),
  `SyntheticNamesRosterTests` (3).
- Outbound-send-boundary guard (7):
  `IsDemoDealershipTests` (3),
  `SuppressIfDemoTests` (4).
- **Outbound egress scanner (1)**:
  `OutboundEgressScannerTests` — scans
  `services/**/*.py` for egress patterns
  (requests./httpx./smtplib/django.core.mail);
  asserts each match is either behind the
  guard or on the LLM allowlist. Fails loud
  if a future adapter forgets the guard.
- Registry create/reset/list (5):
  `CreateDemoStoreTests` (1),
  `ResetDemoStoreGuardTests` (3),
  `ListDemoStoresTests` (1).
- Archetype dispatcher (3):
  `ArchetypeDispatcherTests`.
- ScenarioSummary shape (2):
  `ScenarioSummaryTests` — defaults empty
  tuples; frozen enforcement.
- Management command (8):
  `DemoStoreCommandTests` — list empty;
  list present; create surfaces stub error;
  reset missing dealership errors; reset
  refuses non-demo; export CSV to stdout;
  since filter applies; bad since errors.
- Permission-class zero-drift set equality
  (1):
  `M181PermissionClassZeroDriftTests`.
- Endpoint count unchanged (1):
  `M181EndpointCountTests`.

## Baseline delta

- **Backend: 4,363 → 4,416 pass**, 1
  skipped, 0 fail. **+53 tests, 0
  regressions.** Exceeded 30-40 planning
  target by 13 due to the outbound-scanner
  test + management-command coverage.
- Migrations 0043-0046 → **0043-0047** (+1
  at M18.1).
- Tenancy carriers 49 → **50** (added
  TesterFeedback).
- DRF admin surface **107** (unchanged —
  feedback POST endpoint lands at M18.5).
- Frontend operator routes **20**
  (unchanged — M18 introduces zero new
  operator routes per §5.f + Q7).
- Permission classes **7 actual** —
  **zero-drift streak now ten consecutive
  milestones** (M10 → M18.1).
- Celery-beat task families **10**
  (unchanged — no beat entry at M18).
- Frontend Vitest **140** (unchanged — no
  frontend at M18.1).

## Streak update

**77 planning-time as-recommended M5.1 →
M18.0** (unchanged — M18.1 is
implementation-time work per M10 §9). One
§0.a M18.1 implementation-time decision
recorded (outbound-send-boundary
enumeration + posture).

## What's next: SESSION_148 M18.2 retail/subprime archetype pack

Per `MILESTONE_18_PLANNING.md` §7 M18.2:

- `services/demo_store/archetypes/retail_subprime.py`
  — replace the stub `NotImplementedError`
  with the atomic `build()` verb.
- Construct ~20 vehicles ($8k-$18k, used
  only, 2013-2019 mixed makes).
- 4 salespeople (sales manager + 3
  advisors).
- Sales pipeline: ~15 active leads across
  stages; ~5 recent Sales (cash + retail-
  finance mix); 1 BHPH Sale exercising the
  M15 sync-sibling GL post.
- Recon activity: 3 in-recon vehicles with
  VehicleCost history + ConditionFindings +
  WorkOrders.
- F&I: 2 recent CreditApplication rows with
  sub-prime lender routing; 1 Chargeback
  for M18 audit visibility.
- Follow-up cadences: 4 scheduled tasks
  (guarded by outbound-send guard).
- Fix UI defects only per §5.f Option A —
  blocking or materially misleading. Each
  fix commits with the scenario it unblocks
  in the message.
- Focused tests (~15-20 target) in
  `tests/test_m182_retail_subprime_archetype.py`.

**Backend baseline target at M18.2 close:**
4,416 → ~4,431-4,436 pass (+15-20 tests).

## Anchors that win on conflict

1. `docs/PROJECT_RULES.md`
2. `docs/DOC_GOVERNANCE.md`
3. `docs/roadmap/IMPLEMENTATION_ROADMAP.md`
4. `docs/roadmap/AUTHENTICATION_MODEL.md`
5. `docs/roadmap/MILESTONE_18_PLANNING.md`
   (active memo)
6. `docs/handoffs/SESSION_146_m18_inc0_planning.md`
7. `docs/CAPABILITY_MATRIX.md` §7r
8. `docs/research/INDEPENDENT_DEALER_PIVOT.md`
   (persona shape)
9. `backend/dealer_ai/services/demo_store/`
   (substrate freshly shipped)
10. `backend/dealer_ai/tests/test_m181_demo_store_substrate.py`
    (test coverage document)
