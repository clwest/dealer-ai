---
title: "Dealer AI Kit — Session Start"
status: active
hand_written: true
---

# Dealer AI Kit — Session Start

> Hand-written orientation index. **Not** regenerated by `context-kit adopt`.
> Read this first every session. If a generated doc disagrees with this file,
> verify against the latest handoff and current code before trusting either.

## Product framing (2026-07-31 pivot)

> The kit's **default dealer persona is Copper Canyon Auto**
> (Yuma, AZ — invented independent, mixed-make used only) as of
> SESSION_030 Phases 1–3 and SESSION_031 Phases 4–5. Franchise
> dealers (e.g. Sam Wampler's Dealer OS) remain a supported
> *alternate configuration* via env / setting overrides:
>
> - `DEALER_AI_DEALER_TYPE=franchise`
> - `DEALER_AI_PRIMARY_MAKE=<OEM>`
> - `DEALER_AI_DEALER_NAME=<name>`
>
> Everything below describes the shipped default (Copper Canyon).
> Franchise-config behavior is the same contracts with different
> vocabulary — see the "Franchise config (alternate)" appendix in
> each anchor doc, plus `docs/demo/FREEDOM_FORD_DEMO_SCRIPT.md`
> for the franchise reference demo.

## What this project is

The **Dealer AI Kit** — a reusable dealer-facing AI platform that
ships as an independent-dealer default (Copper Canyon Auto). It's
a budget-constrained vehicle-recommendation chat assistant running
on OpenAI `gpt-5-mini` (Ollama llama3.2 also supported via
`DEALER_AI_LLM_PROVIDER=ollama`). Architecture:

- Deterministic backend math — payment engine (standard-APR + BHPH
  weekly/biweekly variant), candidate classifier, inventory selection.
- 16-stage post-LLM scrub pipeline including indie-prohibited-copy
  scrub (blocks "brand new", "CPO", "certified pre-owned",
  "manufacturer warranty", OEM-captive finance names, "0% APR"
  when `dealer_type == "independent"`).
- State-layer enforcement (intent-shift reset, sticky cash mode,
  weak-intent budget inference).
- Tightly prompted LLM (`INDIE_MODE_HINT` fragment injected for
  indie configs).
- Frontend: live banner, per-message framing label, brand-token
  theming via `useBrand()` → `DEFAULT_DEALER` (Copper Canyon).

## Current baseline

Verified 2026-08-02 (after SESSION_105 M9 closeout). Update if any row changes.

| Surface | State |
|---|---|
| Backend tests | **3730 passed, 1 skipped, 0 failed** (`cd backend && python3 manage.py test dealer_ai`) |
| Frontend tests | **51 passed** (`cd frontend && npm test`) — grew from M9.5 baseline (34) by +17 at M10.7 |
| Milestones shipped | M1 (multi-tenancy + auth), M2 (investment ledger), M3 (structured condition report), M4 (recon automation), M5 (vehicle lifecycle stages + retail gating), M6 (photography + listing generation), M7 (async infrastructure — SESSION_093), M8 (operational intelligence — SESSION_099), M9 (sale + delivery closure — SESSION_105), **M10 (F&I deal desk — SESSION_113)** |
| M5 lifecycle surface | 2 models + migration `0017` (bootstrap seeds every existing Vehicle to `frontline`/`off_market` + matching event) + 12-stage vocabulary + 4-trigger vocabulary + `services/vehicle_lifecycle.py` (5 fns + 4 distinct domain errors + 3 rule evaluators + 1 dataclass + read helper + annotation helper) + 2 Vehicle `@property` accessors + 3 M5.4 admin endpoints + M5.6 operator UI (`/dealer-ai-inventory/:stock/lifecycle`) + `customer_visible_vehicles()` filters on `stage=frontline` (choke-point flip; every retail-side surface inherits) |
| M6 photo + listing surface | 2 models (`VehiclePhoto`, `VehicleListing`) + migrations `0018` (additive) + `0019` (public_id backfill) + `services/photo_gallery/` package (6 verbs + 4 domain errors + 3 module constants — 1024×768 dimension threshold + 8 photo count threshold; restructured from flat module to package at SESSION_092 M7.5 for reaper sibling) + `services/vehicle_listing.py` (5 verbs + 5 domain errors + M4.5-shape source-bundle assembly + LLM factory integration) + `services/photo_storage.py` extended (`store_vehicle_photo`, `put_bytes` on both adapters, `build_canonical_vehicle_photo_key`, `parse_canonical_vehicle_photo_key`, `delete_vehicle_photo_object` sibling added at M7.5) + `services/llm_safety.py::_RECON_COMM_KINDS` extended 2 → 3 (dispatch-only) + M6.4 rules (`_rule_photography_to_listing` filled + `_rule_listing_to_frontline` new) + 13 M6.5 admin/showroom endpoints + 2 M6.5 operator UI routes (`/dealer-ai-inventory/:stock/photos`, `/listing`) + SESSION_075 §5.i truthful-language refactor (customer-lookup path requires frontline + published listing) |
| M7 async substrate | Celery 5.5.3 + Redis 6.4.0 + `django-celery-beat` 2.8.1 DatabaseScheduler. `backend/dealer_kit/celery.py` + settings block (`REDIS_URL` → broker + result backend; `CELERY_TASK_ALWAYS_EAGER=_is_running_tests()`; JSON-only serialization pins). Cross-cutting `@instrumented_task` decorator (`services/jobs/instrumentation.py`) with retry-on-transient-error policy. `JobRunLog` model + migration `0020` (composite `(task_name, -started_at)` index). Four scheduled job families at hourly cadence: **02:00** floor-plan interest accrual (`services/floor_plan/`) — extracted M2 command body verbatim; **03:00** aging-per-stage snapshot (`services/lifecycle_aging/`) with new `StageAgingSnapshot` model + migration `0021` (composite `(dealership, stage, -snapshot_at)` index); **04:00** vendor SLA warnings (`services/vendor_sla/`) — read-only, emits `logging.WARNING` per breach (three locked policy constants: 7-day approved-stale, 0-day ETA grace, outsourced-only); **05:00** photo tombstone reaper (`services/photo_gallery/reaper.py`) — 30-day retention, storage-first delete, iteration-level failure isolation. Every task wraps `@instrumented_task` writing one `JobRunLog` per invocation. |
| M8 operational intelligence | `services/analytics/` package (4 submodules) shipping 6 aggregations across 6 DRF endpoints under `/api/dealer-ai/admin/analytics/` — Q1 recon per source, Q2+Q4 vendor performance, Q5+Q9 stage aging trend, Q10 SLA breach patterns, Q3 proxy vehicle-type recon cost, Q8 proxy days-at-frontline. `SlaBreachRecord` model + migration `0022` (composite `(dealership, kind, -detected_at)` index + unique `(work_order, kind, detected_at_date)` anchoring M7.4 daily-scan idempotency); M7.4 `detect_sla_breaches` verb extended additively to write rows via `get_or_create` (log warning contract preserved). Operator UI at `/dealer-ai-analytics/` (4 tabs — Acquisition & Recon Cost / Vendor Performance / Lifecycle Aging / SLA Breach Patterns) with recharts. **First frontend test infra in the project's history** — Vitest + `@testing-library/react` + `jsdom` + `@testing-library/jest-dom` + `@testing-library/user-event`. Q6 (gross-profit trend) + Q7 (buyer estimate accuracy) deferred to Milestone 9 pending Sale + acquisition-buyer-provenance substrate; Q3 + Q8 shipped as proxies pending the same Sale substrate. `AnalyticsCache` materialization deferred per §5.a Option C hybrid pending operator latency evidence. |
| M9 sale + delivery closure | Two new entities — `Sale` (M9.1, migration `0023_sale_entity_and_buyer_fk` — same migration adds `VehicleAcquisition.buyer` FK nullable) + `Delivery` (M9.2, migration `0024_delivery_entity`, mandatory OneToOne with Sale). Two new service packages — `services/sale/` (`gross_realized` pure verb + `record_sale` transactional write denormalizing `gross_realized` at insert time) + `services/delivery/` (`record_delivery` + `update_checklist_item` + `verify_insurance` atomic column-and-key). Four "true" analytics verbs closing M8 deferrals — `vehicle_type_profitability` (Q3 true), `gross_profit_trend` (Q6, new `services/analytics/gross_profit.py` module), `buyer_estimate_accuracy` (Q7, reads M9.1 buyer FK), `inventory_turn` (Q8 true, reads M5 `VehicleStageEvent.entered_at`). M8.4 proxy verbs preserved with smoke tests locking their shapes unchanged. Seven new DRF endpoints — one M9.1 Sale POST (with GET dispatch added at M9.5 via `@api_view(["GET", "POST"])` method-multiplex), two M9.2 Delivery (POST + PATCH; POST also GET-dispatched at M9.5), three M9.3 analytics extensions, one M9.4 Q7 endpoint. Fifth "Realized Gross" tab on `/dealer-ai-analytics/` wrapping Q3/Q6/Q7/Q8. New per-vehicle page at `dealer-ai-inventory/:stock/sale/` (three render states: no-Sale create-form → Sale-no-Delivery start-button → Sale+Delivery checklist). `LeadVehicleInterest.stage_at_interest` annotation deferred (substrate gap — through-model doesn't exist; SESSION_103 §0.a). |
| M10 F&I deal desk | Ten new entities across seven implementation sessions — `CreditApplication` (M10.1, migration `0025`, nullable FKs to both `CustomerLead` and `Sale`; retention clock locked at model layer, 7 years per FINANCE §6.9) + `DealStructure` (M10.2, migration `0026`, mandatory FKs to `CreditApplication` + `Vehicle`; three denormalized ratio outputs LTV/PTI/DTI populated at write; bundled additive extension of M10.1 CA with `gross_monthly_income` + `existing_monthly_debt`) + `LenderProgram` + `LenderSubmission` (M10.3, migration `0027`, per-dealership lender catalog + submission with fixed 4-value status; `PROTECT` FK on lender_program is a first-for-project deactivate-not-delete pattern via `is_active`) + `Stipulation` (M10.4, migration `0028`, mandatory FK to `LenderSubmission`; fixed 5-value stip_type + 3-value state vocab; `documented_by` User FK SET_NULL sourced from `request.user` server-side — new audit-trail pattern) + `Contract` + `BackEndProductAgreement` + `Funding` (M10.5, migration `0029`, three entities in one increment; Contract three-state machine unsigned → signed → voided with two-verb transition pattern; BEPA per-product rows enable per-product chargeback attribution at M10.6; Funding OneToOne with mark_funded auto-populate) + `Chargeback` (M10.6, migration `0030`, nullable FKs to both Contract and BEPA; atomic cross-model side effects — deal-level chargebacks auto-transition Funding to `chargedback`, product_cancellation auto-populates BEPA cancellation fields; `net_realized(sale)` pure aggregate verb per §5.c Option B — no M9 schema change) + `ComplianceRecord` (M10.7, migration `0031`, OneToOne per Contract; single-entity typed-columns model covering FINANCE §6.1-§6.9 concerns; retention denormalized from parent CA for deal-jacket query-ability). One complete new `services/f_and_i/` package with seven submodules. One new `IsFinanceManagerOrOwnerAtActiveDealership` permission class at M10.1 (reused unchanged M10.2-M10.7; zero drift). Seventeen new DRF endpoints. First F&I frontend surface at M10.7 — `/dealer-ai-f-and-i/` two-tab MVP (deals-in-progress list + per-deal compliance-audit view with seven mark-timestamp actions). Photo/document upload plumbing deferred through M10.4/M10.5/M10.7 as post-M10 initiative. Twenty-nine load-bearing decisions resolved across seven implementation sessions, all confirmed as-recommended — new lesson: streak-pattern signals trust, not correctness (retrospective §6 lesson 16). |
| Tenancy carriers | 34 (M1 six + M3 three + M4 six + M5 two + M6 two + M7 two + M8 one + M9.1 one — `Sale` + M9.2 one — `Delivery` + M10.1 one — `CreditApplication` + M10.2 one — `DealStructure` + M10.3 two — `LenderProgram` + `LenderSubmission` + M10.4 one — `Stipulation` + M10.5 three — `Contract` + `BackEndProductAgreement` + `Funding` + M10.6 one — `Chargeback` + M10.7 one — `ComplianceRecord`) |
| DRF admin endpoints | 64 (21 pre-M6 + 13 M6.5 + M7 zero + 6 M8 analytics + 7 M9 + 17 M10 — 1 M10.1 credit-app + 1 M10.2 deal-structure + 3 M10.3 lender + 2 M10.4 stip + 5 M10.5 contract/BEPA/funding + 1 M10.6 chargeback + 4 M10.7 compliance/deal-jacket/deals-list) |
| Frontend operator routes | 11 (5 pre-M6 + 2 M6.5 + 1 M8.5 `/dealer-ai-analytics/` + 1 M9.5 `dealer-ai-inventory/:stock/sale` + 2 M10.7 `/dealer-ai-f-and-i` + `/dealer-ai-f-and-i/:contract_id/compliance`) |
| Public endpoints | +1 M6.5 showroom (`GET /api/dealer-ai/showroom/vehicles/<stock_number>/`) |
| Frontend | typecheck + build clean (`cd frontend && npx tsc --noEmit && npx vite build`); Vitest suite green (`cd frontend && npm test`) |
| LLM | OpenAI `gpt-5-mini` (API key in repo-root `.env`); Ollama llama3.2 supported via provider env |
| Frontend dev | Vite @ `:5173`, proxy to backend @ `:8001` via `VITE_API_PROXY_TARGET` |
| Django project package | `backend/dealer_kit/` (was `backend/freedom_ford/` pre-SESSION_031) |
| Default dealer | Copper Canyon Auto (Yuma, AZ) — 12-unit sample inventory in `frontend/src/data/sampleInventory.ts`; 45-unit backend seed via `python3 manage.py seed_copper_canyon_demo` |
| Onboarding | page at `/dealer-ai-onboarding` — persisted via `GET\|PUT\|PATCH /api/dealer-ai/onboarding/profile/` (singleton, one-store); fields shape live chat (greeting/tone/approved/banned/disclaimer/escalation flow into system prompt + post-LLM scrub stack). Indie-specific fields (`dealer_type`, `bhph_enabled`, `subprime_lenders`, `floor_plan_lender`, `warranty_offering`, `credit_range_served`, `makes_carried`) sketched in `DealerProfile` resolver but not yet persisted to `DealerOnboardingProfile` migration. |
| Manager chat tester | page at `/dealer-ai-manager-chat` ("Test Assistant Coaching Mode"), endpoint `POST /api/dealer-ai/manager-chat/`. Coaching/preview mode with structural enforcement (Shape A qualifying questions or Shape B coaching directive). Stateless; sessions tagged `channel=manager_test`. |
| Franchise demo (alternate config) | Still runnable via env overrides — see `docs/demo/FREEDOM_FORD_DEMO_SCRIPT.md` |

## Read order (every session)

1. `docs/DEALER_KIT_SESSION_START.md` — this file
2. `00-START-NEXT-SESSION.md` — current session priority
3. Latest `docs/handoffs/SESSION_NNN_*.md` — what last session shipped
4. `docs/DEALER_KIT_BEHAVIOR_LAYER.md` — voice / UI / constraint contract
5. `docs/demo/COPPER_CANYON_DEMO_SCRIPT.md` — canonical indie demo (5-prompt flow)
6. `docs/research/INDEPENDENT_DEALER_PIVOT.md` — pivot plan + status snapshot
7. `docs/research/VEHICLE_CENTRIC_PIVOT.md` — vehicle-centric OS pivot plan (proposed)

Supporting (read on demand):

- `docs/PROJECT_PIPELINE.md` — runtime flow map (entry points, scrubs, state surfaces)
- `docs/CAPABILITY_MATRIX.md` — verified capability matrix per prospect claim
- `docs/DEALER_KIT_TRANSLATION_LAYER.md` — audience contract per persona
- `docs/onboarding/FREEDOM_FORD_ONBOARDING_PLAN.md` — onboarding architecture (still Freedom-Ford-flavored file name; content dealer-agnostic)
- `docs/roadmap/ASSISTANT_AGENT_CREATION_ROADMAP.md` — future-entity roadmap
- `docs/demo/FREEDOM_FORD_DEMO_SCRIPT.md` — franchise alternate-config demo

## Current next task

**SESSION_032 — open.** See `00-START-NEXT-SESSION.md` for the
hand-written priority block. SESSION_031 (Phase 4 Django rename +
Phase 5 doc refresh) shipped on 2026-07-31 — handoff at
`docs/handoffs/SESSION_031_pivot_phase4_5.md`.

## What to skip or treat carefully

- `docs/CONTEXT_KIT_INVENTORY.md` — low-signal except the `Tracked files` row. Don't quote any other count from it as a project stat.
- `docs/PROJECT_WHAT_IT_IS.md`, `docs/BUILD_PLAN.md`, the adopt-managed block in `CLAUDE.md` — auto-generated boilerplate. The one-paragraph project blurb is the signal; the rest either duplicates this file or asks already-answered onboarding questions.
- **Demo behavior** — don't reopen voice, the scrub stack, or state-layer logic unless a regression is observed in the 5 Copper Canyon scenarios (`docs/demo/COPPER_CANYON_DEMO_SCRIPT.md`) or the franchise reference demo.
- **LLM provider, `payment_engine`, `_classify_candidates`, inventory selection, indie-prohibited-copy scrub** — locked. No changes without explicit approval.
- **Franchise config path** — do not delete. Franchise stays a supported *alternate* via `DEALER_AI_DEALER_TYPE=franchise` + `DEALER_AI_PRIMARY_MAKE=<OEM>`.

## Smoke checks before declaring done

If the change touched **the backend**:

```bash
cd backend && python3 manage.py test dealer_ai
```

Expect: `3730 passed, 1 skipped, 0 failed`.

If the change touched **the frontend**:

```bash
cd frontend && npx tsc --noEmit && npx vite build && npm test
```

Expect: 0 errors from `tsc` / `vite`; **51 passing** from Vitest
(baseline established at M8.5 SESSION_098 with 19; extended to
34 at M9.5 SESSION_104 with the Realized Gross tab +
VehicleSalePage components; extended to 51 at M10.7 SESSION_112
with the F&I two-tab MVP — `DealerFandIDeals` +
`DealerFandICompliance`).

If the change touched **anything that can affect chat output**:

Run the 5 canonical scenarios in `docs/demo/COPPER_CANYON_DEMO_SCRIPT.md` against the live LLM. All five must still produce the documented shape (cards render, no debug stocks, no financing language in cash mode, no brochure copy in model followups, drivetrain labels standardized, no OEM-brand-specific language in indie default). If the change might affect the franchise path, run the alternate-config demo in `docs/demo/FREEDOM_FORD_DEMO_SCRIPT.md` with `DEALER_AI_DEALER_TYPE=franchise` set.

## When this file is wrong

This file is hand-written and can drift. If a fact here disagrees with current
code, the latest handoff in `docs/handoffs/`, or live test output — **trust the
runtime side** and update this file. Don't update the handoff to match this file.
