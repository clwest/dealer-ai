---
state: active
date: 2026-05-01
---

<!-- context-kit:adopt:start -->
<!-- This block is managed by `context-kit adopt`. Edits between the markers are overwritten on re-run. Edits outside the markers are preserved. -->

# Next session — Freedom Ford

> Generated by `context-kit adopt` on 2026-05-02.

## Agent Launch Prompt

Paste this into Claude Code, Cursor, or any AI coding agent to start safely.

```text
You are working in a project that context-kit just adopted. Before writing any code, do the following.

WHAT THIS PROJECT APPEARS TO BE
Full-stack web app (medium confidence)
Reasoning: Recognized split monorepo with a Python backend (backend/) and a JavaScript frontend (frontend/).

USER CONTEXT (provided during adopt)
- Project (per the user): Freedom Ford Dealer AI: budget-constrained vehicle selection on Ollama llama3.2 (3.2B); deterministic backend math + post-LLM scrub pipeline + state-layer enforcement; manager coaching tester with structural enforcement (Shape A or Shape B only); onboarding foundation persisting to DB; shadcn UI primitives bridged onto Tailwind v3
- Next task (per the user): SESSION_012 — UI redesign with shadcn primitives + Playwright verification

DISCOVERED NOTES / CONTEXT
The user provided these notes from prior inspection or context:
- SESSION_011 shipped: structural coaching enforcement for manager chat, Live Chat Mode contract in TRANSLATION_LAYER, shadcn primitives installed with Tailwind v3 bridge. Backend baseline 1210 pass / 1 skip. Next focus is frontend Dealer OS shell and visual polish.

PRIMARY DETECTION
Split monorepo — backend=Python, frontend=JavaScript / Node.js

WORKSPACE / PROJECT STRUCTURE
- No workspace child projects detected.

RECOMMENDED FIRST ACTION
Inspect the backend/frontend boundary first.
- Read the backend code to understand the API surface, models, and ownership.
- Read the frontend workspace to see how it consumes the backend.
- Confirm the local-dev startup order (which side starts first, on what ports) and the deployment boundary.

HOW TO APPROACH THIS REPO
If the project structure is not immediately clear, or the confidence shown above is low, do a structured read-through before proposing tasks: start with entry points (README, main files, routing, configs), map the system (API, frontend, integrations, data flow), and call out inconsistencies, risks, or missing pieces you find.

If confidence is medium, do a quick inspection first — read the README, the top-level structure, and one or two main files — then decide whether the project needs deeper analysis or whether you can move directly to a concrete first task.

If the structure IS clear and confidence is high, skip the read-through and go directly to identifying the highest-value next task — don't pad the response with unnecessary inspection steps.

WHAT TO PRIORITIZE
Prefer identifying real risks, inconsistencies, missing wiring, or unused / incomplete features over surface-level tasks. Do not default to documentation updates unless the user explicitly asked for them.

SAFETY INSTRUCTIONS
- If generated docs exist, read BUILD_PLAN.md, PROJECT_WHAT_IT_IS.md, and CLAUDE.md before making changes. If they do not exist yet, read README, package/manifests, and inspect the detected project structure first.
- Do not change the project's stack (e.g. swapping frameworks, languages, or build tools) without asking the user first.
- Do not run destructive commands (rm -rf, force-push, drop database, schema deletes, etc.).
- Ask before broad refactors that touch more than 3-5 files.
- If the generated docs are missing, do not treat that as an error; continue with repo inspection and propose the first safe task.

TO PRESERVE THIS CONTEXT
After your initial inspection, end your response with a short copy-paste command the user can run to persist this context:

  context-kit adopt . --write \
    --project-summary "<refined one-sentence project summary>" \
    --next-task "<recommended next task>" \
    --notes "<key findings from this inspection>"

When filling in the placeholders:
- Keep notes concise but specific. Include real risks, missing wiring, stale docs, dirty git state, or incomplete features.
- Do not include secrets, tokens, or credentials in any value.
- Escape quotes (\") inside values, or rephrase to avoid quotes.
- If no meaningful findings exist, omit --notes.
- Do not run the command yourself — emit it for the user to copy.

When you're ready to begin work, summarize what you read and propose a concrete first task.
```

## What's next

SESSION_012 — UI redesign with shadcn primitives + Playwright verification

## Discovered notes

The user provided these notes from prior inspection or context:

- SESSION_011 shipped: structural coaching enforcement for manager chat, Live Chat Mode contract in TRANSLATION_LAYER, shadcn primitives installed with Tailwind v3 bridge. Backend baseline 1210 pass / 1 skip. Next focus is frontend Dealer OS shell and visual polish.

## How to start the session

1. Read `docs/BUILD_PLAN.md` — confirm the stack matches reality.
2. Read `docs/PROJECT_WHAT_IT_IS.md` (or your project's *_WHAT_IT_IS.md).
3. Note any `[adopt: please describe]` placeholders — these mark context the user hasn't filled in yet. **Do not block on them for read-only inspection.** Proceed with the recommended first action from CLAUDE.md if it's safe and read-only. Only ask the user before making decisions that depend on the missing context.
4. Then begin the work above.

<!-- context-kit:adopt:end -->

---

# Next session priority

> This section is **outside** the adopt-managed block above and is preserved
> across `context-kit adopt` re-runs. It is the source of truth for what
> the next session should focus on; the "What's next" line inside the adopt
> block is regenerated by adopt and may lag behind this section.

> **SESSION_011 shipped 2026-05-02.** Three threads landed in one
> session: (1) **structural coaching enforcement** for manager chat —
> `enforce_coaching_shape()` is a hybrid prompt + post-LLM validator
> that rewrites any reply lacking a coaching frame or carrying
> customer-facing patterns into a context-aware fallback derived from
> the customer's message; replays Jessica's *"i have $400/mo and want
> a sedan"* failure as a regression test; (2) **Live Chat Mode** added
> as a first-class section in `docs/FREEDOM_FORD_TRANSLATION_LAYER.md`
> — trigger phrases, vocabulary contract, refusal rule, grounding
> rule, five-lens translation pattern; (3) **shadcn primitives + 12
> components** installed in `frontend/src/components/ui/` with a
> manual Tailwind v3 ↔ shadcn-v4-preset bridge (six v4-only variants
> silently no-op; constraint documented in `CLAUDE.md` § *Frontend
> stack notes*). Backend baseline: **1210 pass, 1 skipped, 0 failed**
> (was 1189). Frontend: typecheck + build clean. **Playwright MCP
> installed** (`claude mcp add playwright …`); tools become callable
> after the next Claude Code session restart. Handoff:
> `docs/handoffs/SESSION_011_coaching_structure_and_live_chat_mode.md`.

> **SESSION_010 shipped 2026-05-02 (+ hotfix + coaching reframe same
> day).** Manager-side chat tester at `/dealer-ai-manager-chat` —
> renamed UI to **"Test Assistant Coaching Mode"**, product decision is
> *coaching / preview mode*. Stateless `POST /api/dealer-ai/manager-chat/`.
> Handoff: `docs/handoffs/SESSION_010_manager_chat_tester.md`.

## SESSION_012 — open

**Theme: UI redesign — make it feel like a real dealership product.**

**Read first:**

1. `docs/FREEDOM_FORD_SESSION_START.md`
2. `docs/handoffs/SESSION_011_coaching_structure_and_live_chat_mode.md` (latest)
3. `CLAUDE.md` § *Frontend stack notes (project-owned, hand-written)* —
   Tailwind v3 + shadcn bridge constraints, including the six
   v4-only variants that no-op.
4. `docs/FREEDOM_FORD_TRANSLATION_LAYER.md` § *Live Chat Mode* — how
   to talk with Jessica when she re-engages on the redesigned UI.

### Goals

1. **Refactor the 17 hand-rolled domain components** in
   `frontend/src/components/` to compose shadcn primitives underneath
   while preserving each component's business contract:
   - `LeadDetailModal`, `VehicleDetailModal`, `GenerateAdModal`,
     `FollowUpDraftModal`, `LeadCaptureModal` → `ui/dialog.tsx`.
   - `AssignmentDropdown`, `MyLeadsTable` (row actions),
     `RecommendedActions` → `ui/dropdown-menu.tsx`.
   - `StatCard`, `SalespersonCard`, `VehicleCard`, `ChatVehicleCard`
     → `ui/card.tsx` underneath, Ford palette on top.
   - `HandoffQueue`, `MyLeadsTable`, `SalesPipeline` → `ui/tabs.tsx`,
     `ui/badge.tsx`, `ui/separator.tsx` as appropriate.
   - `LeadCaptureModal` success/error feedback → `sonner` toasts.
2. **Standardize the dealer OS shell** — header, side nav, page chrome
   — so every page (`/dealer-ai-admin`, `/dealer-ai-onboarding`,
   `/dealer-ai-demo`, `/dealer-ai-manager-chat`,
   `/dealer-ai-advisor/<slug>`) shares the same frame, spacing, and
   brand surfaces. Ford palette for brand chrome; shadcn neutrals
   (`primary`, `secondary`, `muted`, `accent`) for input/dialog/
   dropdown surfaces.
3. **Verify each redesigned page with Playwright MCP screenshots**
   before reporting done. After Claude Code restart, the
   `mcp__playwright__browser_*` tools should be callable. Open
   `http://localhost:5173/<route>` and capture a screenshot per page.

### Out of scope for SESSION_012

- Tailwind v3 → v4 migration (revisit after the redesign lands).
- New backend endpoints, new agent behavior, new persistence work.
- Authoring code that depends on the six v4-only variants documented
  in `CLAUDE.md` § *Frontend stack notes*.
- Live Chat Mode workflow with Jessica during the redo (re-engage her
  once SESSION_012 ships and there are real before/after screenshots).
- Salesperson-seed → `Salesperson` linkage (carried forward; revisit
  after the redesign).

### Acceptance signals

- All 17 hand-rolled components either compose a shadcn primitive
  under the hood or have an explicit reason recorded in the file
  comment for staying hand-rolled.
- Every page renders without console errors at port 5173.
- Playwright captures one screenshot per redesigned page; visual
  review by the human confirms each shell feels coherent.
- Backend baseline holds at **1210 / 1 skip / 0 fail** (no backend
  churn unless a frontend change exposes a wiring bug).
- `npx tsc --noEmit` and `npx vite build` both clean.

### Pinned smoke scenarios (still relevant)

Any change that touches the chat path must keep these green:

a. *"I want a 4WD truck for $500/mo with $3k down"* — finance flow.
b. *"I am looking for a cheap car I can pay cash for that gets good
   gas mileage"* — cash mode, comparison block fires.
c. *"tell me about the Honda"* — make-fallback model_followup.
d. *"what about the Fusion"* — substring model_followup.

Plus the SESSION_008 / SESSION_009 / SESSION_010 / SESSION_011 backend
smoke:

e. `GET /api/dealer-ai/onboarding/profile/` returns 200 with the
   default shape on a fresh DB; `PUT` of the full payload upserts;
   subsequent `GET` returns the saved values.
f. With a banned phrase saved, a chat reply that would contain the
   phrase has the offending sentence stripped and
   `metadata.banned_phrase_hits` records the match.
g. `POST /api/dealer-ai/manager-chat/` with `{"message": "hi"}`
   returns 200 with `{"reply": <text>}`. Each call creates a fresh
   `ChatSession` tagged `metadata.channel=manager_test`.
h. `POST /api/dealer-ai/manager-chat/` with
   `{"message": "i have $400/mo and want a sedan"}` returns a
   coaching-shaped reply — never *"the card"*, *"in our inventory"*,
   *"I can show you"*, *"fit your budget"*, *"would that be
   something"*. (SESSION_011 regression case.)

```bash
# Backend full suite (must stay green; baseline 1210)
cd backend && source .venv/bin/activate && python manage.py test dealer_ai

# Frontend
cd frontend && npx tsc --noEmit && npx vite build
```
